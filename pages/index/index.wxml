<view class='container'>
  <view wx:if="{{length > 5}}">大于5</view>
  <view wx:else>小于等于5</view>
  <view>{{"hello " + name}}</view>
  <block wx:if="{{isShow? true: false}}">
    <view>view0</view>
    <view>view1</view>
  </block>
  <view id="abc" style='display: none'>view2</view>
</view>